services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chat
    volumes:
      - datos_postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/chat?schema=public
      JWT_SECRET: ${JWT_SECRET:-clave-secreta-jwt-desarrollo-2024}
      CLAVE_CIFRADO: ${CLAVE_CIFRADO:-clave-cifrado-32chars-abcdefgh!}
      NODE_ENV: production
    depends_on:
      db:
        condition: service_healthy

volumes:
  datos_postgres:
